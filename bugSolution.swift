func calculateArea(width: Double, height: Double) -> Double {
    return width * height
}

let area = calculateArea(width: 10, height: 5) // Correct usage

// Correct usage – providing all parameters
let correctArea = calculateArea(width: 12, height: 7) 
print(correctArea) //Output: 84.0